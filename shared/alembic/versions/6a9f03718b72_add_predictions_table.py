"""add predictions table

Revision ID: 6a9f03718b72
Revises: 493113cf244f
Create Date: 2026-05-17 08:05:57.755650

"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "6a9f03718b72"
down_revision: str | Sequence[str] | None = "493113cf244f"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "predictions",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column(
            "model_id",
            sa.Integer(),
            nullable=False,
            comment="Foreign key to models table",
        ),
        sa.Column(
            "predicted_for",
            sa.Date(),
            nullable=False,
            comment="Date being predicted (usually tomorrow)",
        ),
        sa.Column(
            "predicted_at",
            sa.DateTime(timezone=True),
            nullable=False,
            comment="Timestamp when prediction was made",
        ),
        sa.Column(
            "price_at_prediction",
            sa.NUMERIC(precision=10, scale=2),
            nullable=False,
            comment="BTC price when prediction was made (in USDT)",
        ),
        sa.Column(
            "predicted_price",
            sa.NUMERIC(precision=10, scale=2),
            nullable=False,
            comment="Predicted BTC price (in USDT)",
        ),
        sa.Column(
            "actual_price",
            sa.NUMERIC(precision=10, scale=2),
            nullable=True,
            comment="Actual BTC price (filled by evaluator, NULL until evaluated)",
        ),
        sa.Column(
            "evaluated_at",
            sa.DateTime(timezone=True),
            nullable=True,
            comment="Timestamp when evaluation was performed",
        ),
        sa.Column(
            "error_abs",
            sa.NUMERIC(precision=10, scale=2),
            nullable=True,
            comment="Absolute error: |actual_price - predicted_price|",
        ),
        sa.Column(
            "error_pct",
            sa.NUMERIC(precision=5, scale=2),
            nullable=True,
            comment="Percentage error: (actual_price - predicted_price) / actual_price * 100",  # noqa: E501
        ),
        sa.Column(
            "direction_correct",
            sa.Boolean(),
            nullable=True,
            comment="True if predicted direction (up/down) was correct",
        ),
        sa.Column(
            "pnl_simulated",
            sa.NUMERIC(precision=10, scale=2),
            nullable=True,
            comment="Simulated profit/loss from trading strategy (in USDT)",
        ),
        sa.ForeignKeyConstraint(["model_id"], ["models.id"], ondelete="CASCADE"),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index(
        op.f("ix_predictions_model_id"), "predictions", ["model_id"], unique=False
    )
    op.create_index(
        op.f("ix_predictions_predicted_for"),
        "predictions",
        ["predicted_for"],
        unique=False,
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(op.f("ix_predictions_predicted_for"), table_name="predictions")
    op.drop_index(op.f("ix_predictions_model_id"), table_name="predictions")
    op.drop_table("predictions")
    # ### end Alembic commands ###
